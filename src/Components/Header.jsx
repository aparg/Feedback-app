import PropTypes from 'prop-types'

function Header(){
    return (
        <h1>FEEDBACK UI</h1>
    )
}

// Header.defaultProps = {
// }

// Header.propTypes={
//     text : PropTypes.string
// }
export default Header